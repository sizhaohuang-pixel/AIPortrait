# Preview HD Confirmation Design

**Goal:** 在生成结果页点击“高清”按钮时先进行二次确认，并动态展示本次消耗积分；同时让“高清”按钮颜色与其他操作图标统一。

**Approach:** 仅修改 `frontend/src/pages/preview/index.vue`。复用当前页面已有配置加载链路，优先从已有配置接口读取高清积分消耗；点击高清按钮时先弹确认框，用户确认后再继续调用现有高清任务接口。

**Scope:** 不新增页面、不改后端接口协议、不改其他页面按钮样式。
